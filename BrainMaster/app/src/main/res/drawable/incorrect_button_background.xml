<?xml version="1.0" encoding="utf-8"?>
<!-- 1. Envolvemos todo en un RIPPLE -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Color de la onda -->

    <!-- 2. Nuestro layer-list va DENTRO del ripple, en un <item> -->
    <item>
        <layer-list>
            <!-- Capa 1: La Sombra -->
            <item android:top="4dp" android:left="4dp">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/darker_gray" />
                    <corners android:radius="8dp" />
                </shape>
            </item>

            <!-- Capa 2: El Botón Principal -->
            <item android:bottom="4dp" android:right="4dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/incorrect_red" />
                    <corners android:radius="8dp" />
                    <stroke
                        android:width="2dp"
                        android:color="@android:color/black" />
                </shape>
            </item>
        </layer-list>
    </item>

</ripple>